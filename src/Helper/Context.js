import { createContext } from "react";

export const CartItemQuantityContext = createContext({});
export const CartToggleContext = createContext({});
export const CategoryToggleContext = createContext({});
export const CategoryMenuNameContext = createContext({});
export const ArrowPositionContext = createContext({});
export const CartItemDataContext = createContext({});
export const ProductsFilterContext = createContext({})
export const ProductsCategoryContext = createContext({})
export const CategoryTermAmountContext = createContext({})
export const ProductCardAmountInfoContext = createContext({})  
export const CartTotalPriceContext = createContext({})  